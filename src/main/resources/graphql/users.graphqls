extend type Mutation {
    createUser(user: UserInput): Users
}

extend type Query {
    getUsers: [Users]
    findById(id: Long): Users
}

input UserInput {
    name: String
}


type Users {
    id: Int
    name: String
    email: String
    password: String
    role: String
}